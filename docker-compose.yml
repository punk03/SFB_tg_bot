version: '3'

services:
  stroy-forum-bot:
    build: .
    container_name: stroy-forum-bot
    restart: always
    volumes:
      - ./logs:/app/logs
      - ./.env:/app/.env
    environment:
      - TZ=Europe/Moscow
    networks:
      - bot-network

networks:
  bot-network:
    driver: bridge 